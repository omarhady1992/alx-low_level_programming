#include "main.h"
#include <stdio.h>

/**
 * reverse_array-reverses array
 * @n: number of elements in the array
 * @a: input array
 */

void reverse_array(int *a, int n)
{
	int i, j;
	int v;

	for (i = 0; i < n; i++)
		v[i] = a[i];
	for (j = n - 1; j >= 0; j--)
		a[i] = v[j];
}

