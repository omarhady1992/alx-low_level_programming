#include <stdio.h>

/**
 * main - function that prints a text
 * Return: 0 if function executes
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
